# =============================================================
# RespiraZen - Template de Variáveis de Ambiente Firebase
# Copie este arquivo para `.env` e preencha os valores reais.
# NUNCA commite o arquivo `.env` com chaves reais.
# =============================================================

# Chave pública da Web App Firebase
VITE_FIREBASE_API_KEY=your_api_key_here

# Domínio de auth (geralmente <projeto>.firebaseapp.com)
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com

# ID do projeto Firebase
VITE_FIREBASE_PROJECT_ID=your_project_id

# Bucket de storage
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com

# Sender ID (Messaging)
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id

# App ID da Web (começa com 1:)
VITE_FIREBASE_APP_ID=your_app_id

# Measurement ID (opcional) - se quiser ativar analytics, adicione também no firebase.ts
# VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# -------------------------------------------------------------
# Como obter estes valores:
# 1. Acesse https://console.firebase.google.com/
# 2. Selecione seu projeto > Ícone de engrenagem > Configurações do projeto
# 3. Na aba "Suas apps", escolha a app Web (ou crie uma) e copie o objeto firebaseConfig
# 4. Preencha os campos acima
# 5. Salve como `.env` (mesmo nível do package.json)
# 6. Reinicie: npm run dev
# -------------------------------------------------------------

# Para testar se carregou corretamente, abra o console do navegador e verifique se não há aviso:
# [Auth] Firebase não configurado.

